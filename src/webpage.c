/* The page a browser is served. Generated from web/index.html by
 * tools/embed.sh; edit the HTML, then run the script. Committed so the
 * build needs nothing but a C compiler. */
#include <stddef.h>

const char WEBPAGE[] =
    "<!doctype html><meta charset=utf-8><title>vtt</title>"
    "<body style=\"background:#0e0e12;color:#d8d8e0;font-family:monospace\">"
    "vtt remote view: the page is not built yet.</body>";

const size_t WEBPAGE_LEN = sizeof WEBPAGE - 1;
